# Use when npm inside Docker fails (network). Build on host first:
#   cd react-web
#   npm ci
#   npm run build
# Then from repo root: docker compose build web
FROM nginx:1.27-alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY dist /usr/share/nginx/html
EXPOSE 80
