# Production frontend build — loaded when running: npm run build
# Same-origin Docker/nginx deploy: leave VITE_API_BASE_URL empty (nginx proxies /api).
# Separate API host: set full URL, e.g. https://api.yourdomain.com

VITE_API_BASE_URL=
VITE_GOOGLE_MAPS_API_KEY=
